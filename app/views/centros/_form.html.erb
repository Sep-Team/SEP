<%= simple_form_for @centro, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :codigo %>
  <%= f.input :nombre %>
  <%= f.input :direccion %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                centros_path, :class => 'btn' %>
  </div>
<% end %>
